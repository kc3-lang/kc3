quote defmodule Empty do end
defmodule Empty do end
quote defmodule Double do
  def double = fn (x) { x * 2 }
end
defmodule Double do
  def double = fn (x) { x * 2 }
end
quote Double.double(21)
Double.double(21)
