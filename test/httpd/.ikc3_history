ls
HTTPd.connections
Counter.get(* HTTPd.connections)
HTTPd.connections
Counter.get(* HTTPd.connections)
Counter.increase(* HTTPd.connections, 1)
Counter.get(* HTTPd.connections)
KQueue.add(
1, :read, :123)
kq = KQueue.kqueue()
KQueue.add(kq, 0, void, :plop)
