%{module: C3.Facts.Dump,
  version: 1}
add {List, :is_a, :module}
add {List, :name, "List"}
add {List, :path, "list.facts"}
add {List, :symbol, List.map}
add {List.map, :fn, fn {
  (_, ()) {
    ()
  }
  (f, (a | b)) {
    (f(a) | List.map(b))
  }
}}
